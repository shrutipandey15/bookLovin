
const MoodTypes = {
  heartbroken: 1,
  healing: 2,
  enpowered: 3,
}

const UserRoleTypes = {
  standard: 1,
  publisher: 2,
  editor: 3,
}

const ErrorCodeTypes = {
  fatal: 1,
  not_found: 2,
  invalid_parameter: 3,
  already_exists: 4,
  permission_denied: 5,
}

export default { MoodTypes, UserRoleTypes, ErrorCodeTypes }

# Quickstart

To list main commands, just type:

```bash
just --list
```

## Setup

Type `just install`

## Start all

Type `just`

## populate the database

Type `just populate`


## run all the checks

Type `just checkall`
